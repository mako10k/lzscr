# Tuple helpers for stdlib
# Minimal 2-tuple helpers: constructor and accessors

# Construct a 2-tuple as a record with `fst` and `snd` fields
~tuple2 ~a ~b = {fst : ~a, snd : ~b};

# Accessors
~fst ~t = (~t .fst);
~snd ~t = (~t .snd);
